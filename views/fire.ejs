<h1>Fire Signs</h1>

<ul>
    <% fireSigns.forEach((signs) => { %>
        <li><%= signs %></li>
    <% }) %>
</ul>

<h2>Traits</h2>

<ul>
    <% fire.forEach((characterisitcs) => { %>
        <li><%= characterisitcs %></li>
    <% }) %>
</ul>