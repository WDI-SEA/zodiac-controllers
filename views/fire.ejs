     <style>
        body{background-color: red;
            color: white;}
    </style>

    <h2>Fire Signs</h2>
    <ul>
        <% fireTraits.forEach((fire) => { %>
            <li><%=fire%> </li>
        <% }) %> 
      </ul>