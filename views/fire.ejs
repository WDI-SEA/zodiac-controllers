<h2>FIRE</h2>

<h3>signs</h3>
<ul>
    <% fire.forEach(fire => { %>
        <!-- this oone needs wqual sign because want it to be showed up -->
        <li><%= fire %></li>
      
    <% }) %>
</ul>

<h4>traits</h4>
<ol>
    <% traits.forEach(traits => { %>
        <!-- this oone needs wqual sign because want it to be showed up -->
        <li><%= traits %></li>
    
    <% }) %>
</ol>
