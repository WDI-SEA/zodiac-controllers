<%- include ("partials/header.ejs") %>

<p>Fire folks! You are one of these three signs:</p>
<ul>
  <% fireSigns.forEach((sign) => { %>
  <li><%= sign %></li>
  <% }) %>
</ul>
<p>Here are some traits that exemplify you:</p>
<ul>
  <% fireTraits.forEach((trait) => { %>
  <li><%= trait %></li>
  <% }) %>
</ul>
<%- include("partials/footer.ejs") %>
