<h2>Fire</h2>
<ul>
    <% fireTrait.forEach((trait)=>{ %>
        <li><%= trait %></li>
    <% }) %>
</ul>

<ul>
    <% types.forEach((type)=>{ %>
        <a href="/fire/<%= type %>"><li><%= type %></li></a>
    <% }) %>
</ul>