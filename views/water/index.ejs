<h1><%=element%></h1>

<h2>Signs</h2>
<ul>
<% signs.forEach(element => { %>
    <li><%=element%></li>
           <% }) %>
   </ul>
<h2>Traits</h2>
   <ul>
    <% traits.forEach(element => { %>
 <li><%=element%></li>
        <% }) %>
</ul>