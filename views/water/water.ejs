<h1>Water Signs</h1>
<ul>
    <% waterSigns.forEach(element => { %>
        <li><%= element %> </li>
    <% }) %> 
</ul>


<h2>Water Traits</h2>
<ul>
    <% water.forEach(element => { %>
        <li><%= element %> </li>
    <% }) %>
</ul>