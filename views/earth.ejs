<% signs.forEach(sign=>{ %>
    <p>
        <a href="<%= `/earth/${sign.toLowerCase()}` %>"><%= sign %></a>
    </p>
<% }) %>
<% traits.forEach(trait => { %>
    <p>
        <%= trait %>
    </p>
    <br>
<% }) %>