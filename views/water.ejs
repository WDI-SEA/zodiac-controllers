<%- include('layout.ejs') %>

<h1>Water</h1>
<h2>Signs:</h1>
<ul>
    <% signs.forEach(e => { %>
        <li>
            <%= e %>
        </li>
    <% }); %>
</ul>

<h2>Traits:</h2>
<ul>
    <% traits.forEach(e => { %>
        <li>
            <%= e %> 
        </li>
   <% }) %> 
</ul>
<!-- can just use one of these files -->