<% signs.forEach(sign=>{ %>
    <p>
        <a href="<%= `/water/${sign.toLowerCase()}` %>"><%= sign %></a>
    </p>
<% }) %>
<% traits.forEach(trait => { %>
    <p>
        <%= trait %>
    </p>
    <br>
<% }) %>
