<%- include("partials/header.ejs") %>

<h1>Water Signs</h1>
<ul>
    <% signs.forEach((sign) => { %>
       <li> <%= sign %> </li>
    <% }) %>
</ul>
<ul>
    <% traits.forEach((trait) => { %>
       <li> <%= trait %> </li>
    <% }) %>
</ul>

<%- include("partials/footer.ejs") %>