<h1>Water Signs</h1>

<ul>
    <% waterSigns.forEach(waterEle => { %>
        <li><%= waterEle %> </li>
    <% }) %>
</ul>

<h2>Water Traits</h2>

<ul>
    <% water.forEach(element => { %>
        <li><%= element %> </li>
    <% }) %>
</ul>