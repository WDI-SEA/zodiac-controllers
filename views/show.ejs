<%- include ('layout.ejs') %>

<h3><%= element.name %></h3>

<% element.signs.forEach(sign => { %>
    <a href="<%= element.name %>/<%= sign %>"><%= sign %></a>
<% }) %>

<ul>
    <% element.traits.forEach(trait => { %>
        <li><%= trait %></li>
    <% }) %>
</ul>
