<%- include ('layout.ejs') %>

<h3><%= element.name %></h3>

<% element.signs.forEach(sign => { %>
    <ul><%= sign %></ul>
<% }) %>

<ul>
    <% element.traits.forEach(trait => { %>
        <li><%= trait %></li>
    <% }) %>
</ul>
